const handleHttpError = require('../utils/handleHttpError');

//TODO :lo que queremos es, que si eres admin, puedes editar lo que quieras, pero si eres user, modificar tus campos menos tu rol
const checkRoleToModifyUser = (role)  => (req, res, next)=>{
    try {
        
    } catch (error) {
        
    }
}